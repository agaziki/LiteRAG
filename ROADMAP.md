# LiteRAG 路线图

> 当前版本：v1.4.1 · [已交付](#已交付) · [v1.3.x 部署与安全收口 ✅ 全部完成](#v13x--部署与安全收口已完成) · [v1.4.x 检索质量与运营闭环 ✅ 全部完成](#v14x--检索质量与运营闭环已完成) · [下一版本 v1.5](#下一版本-v15) · [v2.0 多租户与实时人工（远期）](#v20--多租户与实时人工远期)

## 已交付

### v1.2.0 — 图片问答 + 转人工闭环 + 话题边界

- 图片问答：点击 / 拖拽（含网页商品图片，服务端抓取含 SSRF 防护）/ 粘贴，视觉模型联动显隐
- 转人工：用户留言 → 后台接入 → 人工客服身份回复 → 状态自动轮询
- 思考链时间线：工具调用人性化展示，原始数据默认折叠
- 管理后台权限控制（ADMIN_PASSWORD）+ 设置归入后台（/admin 直链访问）

### v1.3.0 — 长会话历史管理 + CI 上线 + 真实链路回归

- **话题边界策略**：`.env` 的 `TOPIC_BOUNDARY=strict|open` + 管理后台「系统设置」开关；
  温和模式对无关话题礼貌拒答并引导回业务（双模式系统提示词），开放模式维持常识回答 + 检索优先 + 兜底转人工
- **长会话历史截断**：滑动窗口（`HISTORY_WINDOW`，默认 30 条）+ 窗口外历史 LLM 摘要压缩（≤300 字），
  摘要按会话缓存（`sessions.summary/summary_upto`），生成失败自动降级纯窗口模式
- **GitHub Actions CI**：push(main)/PR 自动执行类型检查 + 构建 + 全量测试（当前 90 项断言）
- **真实 Key 端到端回归**：`npm run e2e:check`，6 项真链路验证（对话/FAQ 工具/视觉识图/边界/转人工）

## v1.3.x — 部署与安全收口 ✅ 全部完成（v1.3.1）

| # | 项 | 状态 | 说明 |
|---|-----|------|------|
| 1 | 真实 Key 端到端回归 | ✅ 已完成 | `npm run e2e:check` 已覆盖 vision 识图、FAQ 工具、思考链、转人工闭环（6/6 通过）；语义检索已用真实硅基流动 Key 补充验证：BGE-M3（1024 维）嵌入与无关键词重叠问法的语义命中均通过 |
| 2 | GitHub Actions CI | ✅ 已完成 | push/PR 自动跑 tsc + npm test + build，已在 GitHub 实际运行并通过（2f830d2） |
| 3 | 容器化 | ✅ 已完成（v1.3.1） | Dockerfile（多阶段构建，NODE_IMAGE 可参数化适配国内镜像源）+ docker-compose（含健康检查）+ .dockerignore；实测构建镜像并运行，健康检查/静态托管全部通过 |
| 4 | `/api/save-env-config` 收口 | ✅ 已完成（v1.3.1） | 配置 `ADMIN_PASSWORD` 后自动纳入管理员鉴权，未配置时保持本地开发可用 |
| 5 | 接口限流 | ✅ 已完成（v1.3.1） | 管理登录连续失败 5 次锁定 15 分钟；`/api/chat` 每 IP 20 次/分钟；内存滑动窗口实现（实测触发） |
| 6 | 图片管线加固 | ✅ 已完成（v1.3.1） | 前端 canvas 压缩（长边 2048px，重编码剥离 EXIF）+ 服务端内容审核钩子（`server/moderation.ts`，`IMAGE_MODERATION=true` 启用，预留扩展点） |

## v1.4.x — 检索质量与运营闭环 ✅ 全部完成（v1.4.0）

| # | 项 | 状态 | 说明 |
|---|-----|------|------|
| 1 | Rerank 两阶段检索 | ✅ 已完成（含真链路验证：bge-reranker-v2-m3 真实区分度 0.829/0.423/0.000） | 粗筛 top-20 → `server/rerank.ts` 精排（SiliconFlow `bge-reranker-v2-m3` 风格 /rerank 接口），综合分 = 精排分 ×20 + 原始分 ×0.1；未配置或失败自动回退原排序 |
| 2 | 知识缺口报表 | ✅ 已完成 | 管理后台「知识缺口」标签页：聚合 other 意图 + 低星评价（含评语）+ 转人工原因，「去补充 FAQ」一键预填跳转知识库管理 |
| 3 | Token 用量统计 | ✅ 已完成 | `stream_options.include_usage` 采集 usage，落库 `token_usage` 表；后台概览 Token/估算成本卡片（`PRICE_*_PER_1M` 折算）、会话详情单会话消耗 |
| 4 | FAQ 导出 | ✅ 已完成 | `GET /api/faq/export?format=json\|csv\|xlsx`，与导入格式互逆（Excel 带 BOM）；管理后台「导出」下拉下载 |
| 5 | 答案引用溯源 | ✅ 已完成 | 回答下方「参考来源（N）」折叠面板，从 search_faq 工具调用结果提取 FAQ 条目/文档片段并去重展示 |
| 6 | 文档切块增强 | ✅ 已完成 | 标题层级路径（「H1 > H2 > H3」）作为块标题；相邻块 60 字符 overlap（计入长度预算）；扫描版 PDF OCR 需外部 OCR 服务/渲染栈，移入 Backlog（空文本 PDF 已有明确报错提示） |
| 7 | 向量规模化 | ⏸ 条件触发 | 按规划"规模上来后引入"——当前全量内存扫描在万级块无压力；触发条件：单库块数 >5 万。引入时优先评估 sqlite-vec |

### v1.4.1 — 数据管理 + 发布工程 + E2E 入 CI

- **数据管理界面** ✅：管理后台「数据管理」标签页——数据规模统计；会话批量清理（全部 / 按天数，级联删除关联记录）；知识库重置（恢复出厂 / 清空，均二次确认）
- **E2E 入 CI** ✅：`.github/workflows/e2e.yml`——每日定时（北京时间 02:00）+ 手动触发，Secrets 注入 DeepSeek/硅基流动 Key，6 项真链路回归自动执行（需在仓库 Settings → Secrets 配置 `DEEPSEEK_API_KEY`，可选 `EMBEDDING_*`/`RERANK_*`）
- **发布工程规范化** ✅：`CHANGELOG.md`（Keep a Changelog 格式）+ 补齐 v1.3.0 / v1.3.1 标签（四版本标签齐备，可追溯可回滚）

## 下一版本 v1.5

| # | 项 | 状态 | 说明 |
|---|-----|------|------|
| 1 | 历史图片回传 | ✅ 已决策：排期实现 | 现状仅当前消息图片进模型；v1.5 改为回传最近 K=2 条带图消息的图片（支持「再看刚才那张图」），成本有上界；`VISION_HISTORY_IMAGES` 开关默认关闭保持现状 |
| 2 | Rerank/语义检索真链路常态化 | ⬜ | Keys 已配置后 e2e:check 即覆盖（E2E 工作流 Secrets 注入后自动生效） |

## v2.0 — 多租户与实时人工（远期）

| # | 项 | 说明 |
|---|-----|------|
| 1 | 用户体系与会话隔离 | 访客识别/登录，支撑多渠道接入 |
| 2 | 实时人工会话 | WebSocket 双向，替代当前的异步人工回复，真人客服实时打字 |
| 3 | 多渠道接入 | 网页挂件嵌入、企业微信、公众号 |
| 4 | 数据看板增强 | token 成本统计、响应时延分布、知识命中率 |
