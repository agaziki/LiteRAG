# LiteRAG 路线图

> 当前版本：v1.3.0 · [已交付](#已交付) · [v1.3.x 部署与安全收口（进行中）](#v13x--部署与安全收口进行中) · [v1.4.x 检索质量与运营闭环](#v14x--检索质量与运营闭环中期) · [v2.0 多租户与实时人工（远期）](#v20--多租户与实时人工远期)

## 已交付

### v1.2.0 — 图片问答 + 转人工闭环 + 话题边界

- 图片问答：点击 / 拖拽（含网页商品图片，服务端抓取含 SSRF 防护）/ 粘贴，视觉模型联动显隐
- 转人工：用户留言 → 后台接入 → 人工客服身份回复 → 状态自动轮询
- 思考链时间线：工具调用人性化展示，原始数据默认折叠
- 管理后台权限控制（ADMIN_PASSWORD）+ 设置归入后台（/admin 直链访问）

### v1.3.0 — 长会话历史管理 + CI 上线 + 真实链路回归

- **话题边界策略**：`.env` 的 `TOPIC_BOUNDARY=strict|open` + 管理后台「系统设置」开关；
  温和模式对无关话题礼貌拒答并引导回业务（双模式系统提示词），开放模式维持常识回答 + 检索优先 + 兜底转人工
- **长会话历史截断**：滑动窗口（`HISTORY_WINDOW`，默认 30 条）+ 窗口外历史 LLM 摘要压缩（≤300 字），
  摘要按会话缓存（`sessions.summary/summary_upto`），生成失败自动降级纯窗口模式
- **GitHub Actions CI**：push(main)/PR 自动执行类型检查 + 构建 + 全量测试（当前 90 项断言）
- **真实 Key 端到端回归**：`npm run e2e:check`，6 项真链路验证（对话/FAQ 工具/视觉识图/边界/转人工）

## v1.3.x — 部署与安全收口（进行中，建议 1~2 周量级）

| # | 项 | 状态 | 说明 |
|---|-----|------|------|
| 1 | 真实 Key 端到端回归 | ✅ 已完成 | `npm run e2e:check` 已覆盖 vision 识图、FAQ 工具、思考链、转人工闭环（6/6 通过）；语义检索待配置硅基流动 Key 后补充验证 |
| 2 | GitHub Actions CI | ✅ 已完成 | push/PR 自动跑 tsc + npm test + build，已在 GitHub 实际运行并通过（2f830d2） |
| 3 | 容器化 | ⬜ 待实现 | Dockerfile（多阶段构建）+ docker-compose；一次性根治 Node 版本/原生模块问题，宝塔用户可直接用镜像 |
| 4 | `/api/save-env-config` 收口 | ⬜ 待实现 | 目前唯一未受保护的写接口——公网部署下任何人可改 API Key 干扰服务；配置了 `ADMIN_PASSWORD` 时自动纳入管理员鉴权 |
| 5 | 接口限流 | ⬜ 待实现 | 管理登录失败延迟/锁定（防爆破）；`/api/chat` 简单 IP 限流（防滥用刷 Key） |
| 6 | 图片管线加固 | ⬜ 待实现 | 上传前前端压缩（长边 2048px，减少 base64 体积）、EXIF 剥离、预留内容审核钩子 |

## v1.4.x — 检索质量与运营闭环（中期）

| # | 项 | 说明 |
|---|-----|------|
| 1 | Rerank 两阶段检索 | 粗筛（现有关键词+向量）取 top-20，再用硅基流动 `bge-reranker-v2-m3` 精排——RAG 命中率提升最明显的标准手段 |
| 2 | 知识缺口报表 | 聚合 other 意图 + 低星评价 + 转人工原因，生成「知识库盲区清单」，一键跳转补充条目（数据已在库，只差聚合展示） |
| 3 | Token 用量统计 | 开启 `stream_options.include_usage` 采集 usage，按会话/每日聚合 token 与成本，后台展示 |
| 4 | FAQ 导出 | JSON / Excel 导出，与现有导入形成备份-迁移闭环 |
| 5 | 答案引用溯源 | AI 回答标注引用的文档/条目，点击可查看原文块 |
| 6 | 文档切块增强 | 相邻块 overlap、扫描版 PDF 的 OCR 支持、按标题层级保留更多上下文 |
| 7 | 向量规模化 | 当前全量内存扫描（万级块无压力），规模上来后引入 sqlite-vec 或 HNSW 索引 |

## v2.0 — 多租户与实时人工（远期）

| # | 项 | 说明 |
|---|-----|------|
| 1 | 用户体系与会话隔离 | 访客识别/登录，支撑多渠道接入 |
| 2 | 实时人工会话 | WebSocket 双向，替代当前的异步人工回复，真人客服实时打字 |
| 3 | 多渠道接入 | 网页挂件嵌入、企业微信、公众号 |
| 4 | 数据看板增强 | token 成本统计、响应时延分布、知识命中率 |
